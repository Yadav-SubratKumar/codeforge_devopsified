# Frontend environment variables
# Copy to .env for local development
# Vite exposes only vars prefixed with VITE_ to the browser

# Leave empty to use relative /api path (works in Docker via Nginx proxy)
# Set to http://localhost:5000/api only for local dev WITHOUT Docker
VITE_API_URL=
VITE_ENV=development
